<div class="category">
  <h2><%= category.title %> <span class="date"><%= category.subtitle %></span></h2>
  
  <%= textile category.content %>

  <% if !category.children.empty? %>
    <%= render :partial => category.children %>
  <% end %>
</div>