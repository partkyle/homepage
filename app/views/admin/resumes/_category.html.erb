<% for category in categories %>
  <div class="category">
	<h3><%= category.title %></h3>
	<% if !category.sub_categories.blank? %>
	    <%= render :partial => 'category', :locals => {:categories => category.sub_categories} %>
	<% end %>
</div>
<% end %>