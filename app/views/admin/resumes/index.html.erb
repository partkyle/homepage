<h1>Listing resumes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Comment</th>
    <th>Created at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @resumes.each do |resume| %>
  <tr>
    <td><%= resume.name %></td>
    <td><%= resume.comment %></td>
    <td><%= resume.created_at %></td>
    <td><%= link_to 'Show', [:admin, resume] %></td>
    <td><%= link_to 'Edit', edit_admin_resume_path(resume) %></td>
    <td><%= link_to 'Destroy', [:admin, resume], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Resume', new_admin_resume_path %>
